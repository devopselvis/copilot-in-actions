{
  "name": "demo-app",
  "version": "1.0.0",
  "description": "Demo app for Copilot Actions tutorial",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified - intentionally passing this step\" && exit 0",
    "build": "echo \"Building the project...\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
